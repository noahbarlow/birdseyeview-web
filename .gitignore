.DS_Store
node_modules/
.env
.env.local
*.log
.netlify/

# Source-photo folders for team members (raw .docx, .heic, .pdf, etc.) — keep them local;
# commit only the processed .jpg portraits at img/team/{slug}.jpg
img/team/*/

# DNN model cache for portrait face-detection processing
.cache/

# Source image library (multi-hundred-MB raw drops) — only the processed
# web-optimized copies in img/hero/ are committed.
New images/
BEVP-design-direction.pdf

# Source trailer masters (multi-hundred-MB raw .mp4) — only the compressed
# 540p H.264 copies in img/video/ are committed.
Previews/
